---
import Layout from '@/layouts/Layout.astro';
import {
  Content as Welcome,
  frontmatter as welcomeData,
} from '@/content/pages/bienvenido.mdx';

const pageData = {
  title: welcomeData.title,
  description: welcomeData.description,
};
---

<Layout {...pageData}>
  <Welcome />
</Layout>
