---
import Layout from '@/layouts/Layout.astro';
import {
  Content as Welcome,
  frontmatter as welcomeData,
} from '@/content/pages/bienvenido.mdx';
import components from '@/components/mdx/wrapper';

const pageData = {
  title: welcomeData.title,
  description: welcomeData.description,
};
---

<Layout {...pageData}>
  <Welcome components={components} />
</Layout>
