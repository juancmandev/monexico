---
import Layout from '@/layouts/Layout.astro';
import {
  Content as About,
  frontmatter as aboutData,
} from '@/content/pages/sobre-monero.mdx';

const pageData = {
  title: aboutData.title,
  description: aboutData.description,
};
---

<Layout {...pageData}>
  <About />
</Layout>
